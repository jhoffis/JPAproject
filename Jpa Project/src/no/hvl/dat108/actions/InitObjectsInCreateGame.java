package no.hvl.dat108.actions;

public class InitObjectsInCreateGame implements Action{

	@Override
	public void run() {
		//Get last attribute in a ongoing games table from the database. Add one to the
		//index. Show that in a text like so: "Create game (" + index + ")"
		//First when you hit the button that says create the button parses the game value.
		//The game points to gamevalues or whatever.
	}

}
